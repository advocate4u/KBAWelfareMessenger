name: Sync Admin Public Key

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-public-key:
    runs-on: ubuntu-latest
    steps:
      - name: Public-key synchronization temporarily disabled
        run: |
          echo "Public-key synchronization is temporarily disabled while the license signing-key architecture is being redesigned."
          echo "No PKCS12/P12/PFX key material is used by this workflow."
          exit 1
