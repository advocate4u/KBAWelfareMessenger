name: Validate Admin License Key

on:
  workflow_dispatch:

jobs:
  validate-key:
    runs-on: ubuntu-latest
    steps:
      - name: License signing-key validation temporarily disabled
        run: |
          echo "License signing-key validation is temporarily disabled while the signing-key architecture is being redesigned."
          echo "No PKCS12/P12/PFX key material is used by this workflow."
          exit 1
